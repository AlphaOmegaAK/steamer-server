const db = require('../models');


const home = (req, res) => {
  return res.status(200);
}
const about = (req, res) => {
  return res.status(200);
};

const contact = (req, res) => {
  return res.status(200);
};


module.exports = {
  home,
  about,
  contact
}